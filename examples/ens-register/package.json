{
  "name": "ens-register",
  "private": true,
  "version": "1.0.1-alpha.3",
  "scripts": {
    "test": "npx vitest",
    "test:ci": "npx hardhat node & CI=true npx vitest; kill $(lsof -t -i:8545) || true; exit $(PIPESTATUS[0])"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@boostxyz/sdk": "workspace:*",
    "@boostxyz/signatures": "workspace:*",
    "@boostxyz/test": "workspace:*"
  }
}
